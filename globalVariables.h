#pragma once#include "raylib.h"using namespace std;// global variables (all declared here for cross-file access)// extern indicates the variable is defined elsewhere than the current fileextern Color green;extern Color darkGreen;extern int cellSize;extern int cellCount;extern int offset;extern double lastUpdateTime;extern const double moveInterval;extern bool elementInDeque(Vector2 element, deque<Vector2> deque);extern bool eventTriggered(double interval);